import React from "react";
import Contextual from "../../contextual/Contextual";

function Menu({ outerRef }) {
  return (
    <Contextual outerRef={outerRef}>
      DELETE
    </Contextual>
  );
}

export default Menu;
